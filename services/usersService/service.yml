services:
  users-service:
    build:
      context: .
    env_file:
      - path: ../../.env
        required: false
      - path: ./.env
        required: false
    environment:
      USERS_DATABASE_URL: ${USERS_DATABASE_URL:-postgresql+asyncpg://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-password}@users-postgres:5432/${USERS_POSTGRES_DB:-usersdb}}
      REDIS_URL: ${REDIS_URL:-redis://users-redis:6379/0}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.users-service.rule=PathPrefix(`/users`)"
      - "traefik.http.routers.users-service.entrypoints=web"
      - "traefik.http.routers.users-service.middlewares=auth@file,users-strip"
      - "traefik.http.middlewares.users-strip.stripprefix.prefixes=/users"
      - "traefik.http.services.users-service.loadbalancer.server.port=8000"
      - "traefik.docker.network=web"
    ports:
      - "${USERS_PORT:-8002}:8000"
    depends_on:
      - users-postgres
      - users-redis
    networks:
      - web
      - users-net

  users-redis:
    image: redis:7
    networks:
      - users-net

  users-postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: ${USERS_POSTGRES_DB:-usersdb}
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 5s
      retries: 5
    volumes:
      - users-postgres-data:/var/lib/postgresql/data
    networks:
      - users-net

networks:
  web:
    name: web
  users-net:

volumes:
  users-postgres-data:
